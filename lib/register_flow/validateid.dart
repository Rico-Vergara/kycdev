import 'package:flutter/material.dart';

class ValidateId extends StatelessWidget {
  const ValidateId({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
